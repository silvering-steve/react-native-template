import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const MarketScreen = () => {
  return (
    <SafeAreaView>
      <Text>Market</Text>
    </SafeAreaView>
  );
};

export default MarketScreen;
